import styled from "styled-components"

export const NavigationContainer = styled.div`
  display: flex;
  flex-flow: row;
  align-items: center;
  width: 100%;
  max-width: 800px;
  margin: 0 auto;
  padding: 20px;

  button {
    margin: 0 0 0 auto;
  }

  .home {
    text-decoration: none;
    color: inherit;
  }

`;