import styled from 'styled-components';

export const PageContainer = styled.div`
  .banner {
    display: flex;
    flex-flow: column;
    padding: 20px 40px;
    .headshot {
      border-radius: 50px;
    }
  }
  
  
`;