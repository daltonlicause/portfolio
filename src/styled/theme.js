import { colors } from "./variables"

export const lightTheme = {
  mainBackground: colors.light,
  secondaryBackround: "white",
  text: colors.dark,
  accent: colors.purple
}

export const darkTheme = {
  mainBackground: colors.dark,
  secondaryBackground: colors.dim,
  text: colors.light,
  accent: colors.green
}